---
title: "entryPoint"
icon: "file"
description: "Path to the main TypeScript declaration file (.d.ts) to document. If not specified, will auto-detect from package.json or common paths."
---

# entryPoint

## Summary

Path to the main TypeScript declaration file (.d.ts) to document. If not specified, will auto-detect from package.json or common paths.

## Signature

```typescript
entryPoint?: string;
```

## Description

Path to the main TypeScript declaration file (.d.ts) to document. If not specified, will auto-detect from package.json or common paths.